<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Task</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex flex-col items-center p-8">

  <div class="w-full max-w-3xl">
    <a href="/" class="text-blue-400 hover:text-blue-500 transition mb-6 inline-block">&larr; Back</a>

    <form action="/edit" method="post" class="bg-zinc-800 p-6 rounded-xl shadow-md border border-zinc-700 flex flex-col space-y-6">
      <h2 class="text-2xl font-bold text-pink-400">Edit Task</h2>

      <!-- Old filename (readonly) -->
      <input
        type="text"
        name="prev"
        placeholder="Previous Name"
        value="<%= filename.replace(/"/g, '&quot;') %>"
        readonly
        class="p-3 rounded bg-zinc-700 text-white outline-none focus:ring-2 focus:ring-pink-500"
        required
      />

      <!-- New filename -->
      <input
        type="text"
        name="new"
        placeholder="New Name"
        class="p-3 rounded bg-zinc-700 text-white outline-none focus:ring-2 focus:ring-pink-500"
        required
      />

      <!-- Updated content -->
      <textarea
        name="description"
        placeholder="Update Task Description"
        class="p-3 h-40 resize-none rounded bg-zinc-700 text-white outline-none focus:ring-2 focus:ring-pink-500"
      ><%= filedata %></textarea>

      <input
        type="submit"
        value="Update Task"
        class="bg-pink-500 hover:bg-pink-600 text-white font-semibold py-2 px-4 rounded cursor-pointer transition duration-300"
      />
    </form>
  </div>

</body>
</html>
